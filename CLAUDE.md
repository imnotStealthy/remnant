# Remnant - CLAUDE.md

For Claude Code.

Follow `AGENTS.md` in this directory — it is the canonical Remnant rules file (startup, shutdown, compression, schema). Everything there applies to Claude Code verbatim.
